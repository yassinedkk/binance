name: Récupération Open Interest Quotidien

on:
  schedule:
    - cron: '0 0 * * *'  # Tous les jours à 03:00 UTC
  workflow_dispatch:     # Permet de le lancer manuellement depuis GitHub

jobs:
  fetch-open-interest:
    runs-on: ubuntu-latest

    steps:
    - name: Cloner le dépôt
      uses: actions/checkout@v3

    - name: Installer Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Installer les dépendances
      run: |
        python -m pip install --upgrade pip
        pip install pandas requests pytz

    - name: Lancer le script de récupération
      run: python btc_oi_fetcher.py

    - name: Commit et push du fichier CSV mis à jour
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "actions@github.com"
        git add btc_oi_cumule.csv
        git commit -m "🗂️ Mise à jour quotidienne - $(date -u +'%Y-%m-%d')" || echo "Aucun changement"
        git push
